import { expect } from "chai";

describe("test arry", function () {
  describe("testtt", () => {
    it("mini tests", function () {
      const x = 6 * 10;
      expect(x).to.equal(60);
    });
  });
});
